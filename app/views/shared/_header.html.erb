<header>
  <nav class="navbar navbar-expand-lg navigation navbar-light" id="mainNav">
    <div class="container px-lg-5 mt-1">
      <%= link_to image_tag('logo.png'), root_path, class: 'logo', 'data-turbolinks': false %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <%= link_to campsites_path, class: "nav-link mr-3 small navbar-color" do %>
              <i class="fas fa-campground"></i> キャンプ場一覧
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to mypage_campsites_path, class: "nav-link mr-3 small navbar-color" do %>
              <i class="fas fa-list"></i>
              <!--<%= image_tag current_user.avatar.url, class: "rounded-circle mb-2 ml-1", size: "25x25" %>-->
              マイページ
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to logout_path, method: :delete, data: { confirm: t('defaults.message.logout') }, class: "nav-link small navbar-color" do %>
              <i class="fas fa-user"></i> ログアウト
            <% end %>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</header>
